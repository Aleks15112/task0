#include<iostream>
using namespace std;
int main()
{ 
	char x;
	cin >> x;
	if (x >= 'a' && x <= 'z')
	{
		cout << char(x - ('a' - 'A')) << endl;
	}
	else if (x >= 'A' && x <= 'Z')
		{
			cout << char(x + ('a' - 'A')) << endl;
		}
		else
		{
			cout << "Not a letter" << endl;
		}

		return 0;
}
