#include<iostream>
using namespace std;
int main()
{
	int price, usb, ram_in_gb, has_ssd;

	cin >> price >> usb >> ram_in_gb >> has_ssd;

	bool = ((price >= 1000 && price <= 1500) && usb >= 3 && ram >= 8 && has_ssd) || ( (price <= 800) && (! has_ssd || (ram_in_gb < 8));

  return 0;
}
